import { EncryptPasswordPipe } from './encrypt-password.pipe';

describe('EncryptPasswordPipe', () => {
  it('should be defined', () => {
    expect(new EncryptPasswordPipe()).toBeDefined();
  });
});
