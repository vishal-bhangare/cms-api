import { ConfirmPasswordPipe } from './confirm-password.pipe';

describe('ConfirmPasswordPipe', () => {
  it('should be defined', () => {
    expect(new ConfirmPasswordPipe()).toBeDefined();
  });
});
